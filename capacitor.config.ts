import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Attendify.com',
  appName: 'Attendify',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
